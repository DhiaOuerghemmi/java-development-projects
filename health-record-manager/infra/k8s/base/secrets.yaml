apiVersion: v1
kind: Secret
metadata:
  name: phr-credentials
  namespace: phr-system
type: Opaque
data:
  # base64-encoded values: echo -n 'username' | base64
  DB_USERNAME: REPLACE_BASE64_USERNAME
  DB_PASSWORD: REPLACE_BASE64_PASSWORD
  JWT_SECRET:   REPLACE_BASE64_JWT_SECRET
